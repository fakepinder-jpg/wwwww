# dependances node
node_modules/

# fichiers de build du frontend
frontend(react)/build/
frontend(react)/.env.local
frontend(react)/.env.development.local
frontend(react)/.env.test.local
frontend(react)/.env.production.local

# fichiers strapi a ne pas partager
Backend(strapi)/.env
Backend(strapi)/build/
Backend(strapi)/.tmp/
Backend(strapi)/exports/

# base de donnée sqlite (contient les vrais données)
Backend(strapi)/.tmp/data.db
*.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# fichiers de l'editeur
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# fichiers systeme
.DS_Store
Thumbs.db
desktop.ini

# variables d'environement (important a ne jamais commit!)
.env
.env.*
!.env.example
